<?php

use Tests\TestCase;

require_once __DIR__ . '/bootstrap.php';

/*
|--------------------------------------------------------------------------
| Test Case
|--------------------------------------------------------------------------
*/

pest()->extend(TestCase::class)->in('Feature', 'Unit');

/*
|--------------------------------------------------------------------------
| Setup
|--------------------------------------------------------------------------
|
| ログイン・DB周りのテストはすべて、使い捨てのSQLiteとまっさらなセッションで
| 実行する。n3s_test_setup() は tests/bootstrap.php で定義している。
|
*/

uses()->beforeEach(function () {
    n3s_test_setup();
})->in('Feature', 'Unit');
